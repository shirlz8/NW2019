<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shirleyzeng.nw2019">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".cuisinePreferences"
            android:label="@string/title_activity_cuisine_preferences" />
        <activity android:name=".UserHomePage" />
        <activity
            android:name=".groupSideBarMenu"
            android:label="@string/title_activity_group_side_bar_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".addUsersToGroup" />
        <activity
            android:name=".hangoutSettings"
            android:label="@string/title_activity_group_side_bar_menu" />
        <activity android:name=".restaurant1info"></activity>
    </application>

</manifest>